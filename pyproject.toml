[project]
name = "pytree-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "equinox>=0.13.6",
    "jax>=0.9.2",
]

[dependency-groups]
dev = [
    "ipython>=9.11.0",
]
